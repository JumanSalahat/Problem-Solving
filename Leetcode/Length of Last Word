class Solution {
public:
    int lengthOfLastWord(string s) {
        int c=0;
        int n=s.size()-1;
        for(int i=s.size()-1;i>=0;i--)
        {
            if (s[i] != ' ') c++;
            else if(s[i] == ' ' && i !=n && s[i+1] != ' ') break;
        }
        return c;
    }
};
